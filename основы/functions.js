/*   let a = prompt("A");
  let b = prompt("B");
function getMin(a, b) {

  if (a > b) {
    return b;
  }
  else if(a<=b) {
    return a;
  }
}
let result = getMin(a, b);
  alert(result); */

/* let x = prompt("X");
let n = prompt("n");
function pow(x, n) {
  if (n >= 1) {
    return x ** n;
  }
}
let result = pow(x, n);
alert(result); */


/* let ask = (question, yes, no) =>{
  if (confirm(question)) yes()
  else no()
}
ask(
  "Вы согласны?",
  () => alert("Вы согласились."),
  () => alert("Вы отменили выполнение.")
); */
const ddom = 555;

let salaries = {
  John: 100,
  Ann: 160,
  Pete: 130
};
console.log();
salaries = 5554;
salaries.John = 50;
salaries["Ann"] = 20;
/* salaries = ddom; */
console.log(ddom);
/* let sum = 0;
for (let key in salaries) {
  /*   sum += salaries[key]; */
console.log(salaries);
  


